import { NextHead } from "@components";

export const meta = {
  title: "Sass",
  author: "Bruce",
};

<NextHead title={meta.title} />

# Sass

## 绝对路径引入

在 Sass 中，可以通过 `@use` 指令引入 Sass 模块。

```scss
@use "@styles/theme";
```

Sass 是缩进语法，Scss 是 CSS 超集。
