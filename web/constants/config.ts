import type { AppConfigType } from "@interfaces";

const AppConfig: AppConfigType = {
  name: "Bruce Song",
  brand: {
    imageWidth: 36,
    imageHeight: 36,
    showSkeleton: true,
  },
};

export { AppConfig };
