---
title: 项目
---

export const DOMAIN_SUFFIX = "bruceworld.top"

export const ProjectLink = ({ children, siteUrl, className }) => {
  return (
    <div className={className}>
      <a href={siteUrl}>{children}</a>
    </div>
  )
}

# 项目

<ProjectLink
  siteUrl={`https://wiki.${DOMAIN_SUFFIX}`}
  className="mt-4"
>
  Wiki
</ProjectLink>

<ProjectLink
  siteUrl={`https://taskward.${DOMAIN_SUFFIX}`}
  className="mt-2"
>
  Taskward
</ProjectLink>

<ProjectLink
  siteUrl={`https://resume.${DOMAIN_SUFFIX}`}
  className="mt-2"
>
  Resume Generator
</ProjectLink>
