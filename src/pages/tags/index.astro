---
import { BaseLayout } from '@/layouts'
import { getTags } from '@/utils/server'

const TITLE_TEXT = 'Tags'
const tags = await getTags()
console.log(tags)
---

<BaseLayout
  title={TITLE_TEXT}
  contentTitle={TITLE_TEXT}
>
  <span class="text-xl">Tags page is under development!</span>
</BaseLayout>
